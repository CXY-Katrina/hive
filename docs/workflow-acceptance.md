# 任务编排验收记录

日期：2026-09-12。业务代码与测试夹具分离；以下结果证明通用编排行为，不代表 NPU 性能或精度用例通过。

## 自动化验证

开发按行为逐项执行失败测试、实现、通过测试。基线为 144 项，最终在 Windows 和独立 MySQL 测试库执行全量回归：

```powershell
$env:HIVE_TEST_MYSQL_PORT = "13316"
.venv/Scripts/python.exe -m unittest discover -s tests -q
```

结果：237 项，230 项通过、7 项因平台能力限制跳过，耗时 210.835 秒。GitHub 和调度 SSH 在外部边界模拟，数据库使用随机测试库，不在生产库创建测试申请。

覆盖 PR/SHA/文件冻结、事务幂等、同机独立环境、节点代称、Shell/Python/YAML、服务就绪依赖、资源冲突与并行、环境复用、取消/超时/后置收尾、未知结果保留、容器身份与重启恢复、日志续读与上限、产物完整性与外部判定。并发测试覆盖提交/关闭的竞争及资源锁。

前端 `npm.cmd --prefix frontend run test:browser`：11 项浏览器测试全部通过，TypeScript 检查及生产构建通过。测试拦截全部 API，不提交真实任务。额外将浏览器实际提交的双环境 payload 交给后端 `WorkflowCreate` 校验，通过。

## Linux 实测

在已有 Linux 节点的隔离测试容器内验证通用协议。容器不映射 NPU、宿主机目录或网络，不拉取镜像、不安装业务依赖；测试结束后删除测试容器与临时文件。

- 宿主机进程协议 4 项通过：重复启动、关闭凭证、日志上限、超时及忽略 TERM 的子进程。
- 容器协议 6 组通过：完整身份、幂等准备及变更拒绝、后代进程关闭、超时强制结束、分段日志、身份不符拒绝及容器关闭幂等。
- Linux 日志归档 10 项通过，包含 Windows 跳过的符号链接检查。节点 Python 低于 3.10，仅临时上传的 domain 类型注解延迟求值；归档与测试代码未改动。

原始协议输出保存在本机忽略目录 `data/container-runtime-acceptance.json` 与 `data/workflow-log-acceptance.json`，不将运行数据提交 Git。

## 部署与待接入内容

新增 MySQL 表迁移成功。已构建页面并在 18000 重启 API/worker；上线核验健康接口、现有 5 台节点及新任务接口。生产中没有创建测试任务、空间或预置。

当前预置列表为空。完整 nightly/weekly 清单需从业务 PR 的 JSON 清单导入；管理员仅能启用配置指定的一个样例条目。Qwen3-30B-A3B-W8A8 尚未实机执行，需要实际 PR、部署/测试/校验入口、本地镜像、权重、数据集与可用资源。其余用例不会在样例验收前批量执行。

环境复用要求相同 PR/vLLM SHA 和安装配置，支持更换任务 YAML；目前不支持在空间内单独替换一个环境。软件包字段记录请求值，实际版本由外部核验日志举证。日志和产物当前不自动删除，部署时须为运行数据目录预留容量。


## 同日交互简化与资源映射增量验收

按用户追加要求调整申请与任务的顺序、醒目新建按钮、环境/Jobs Tab、按钮式选项、默认 10 分钟超时、直接上传 + 启动命令、可选前后处理和产物三字段。新任务不再填写端口，依赖只通过图中连线配置。

新增 API 行为先失败后实现，覆盖上传文件按固定 PR 唯一定位、代码不一致拒绝、YAML 配置快照、普通 Shell 变量保留、默认超时、节点映射冻结、镜像别名解析、跨环境同名产物、免类型的通用结果识别。映射另有 6 项真实 MySQL HTTP 测试，覆盖可选纳管、权限、增删、版本冲突、输入校验和容量限制。

统一浏览器测试 22 项通过（表单 16、图 4、映射 2），包括表单实际提交、上传、旧预置兼容、隐藏 Tab 校验、图中服务依赖、明显删除、64 个作业/113 条多分支连线避让、管理员映射编辑、成员只读及窄屏纳管。浏览器真实提交 payload 另经后端 schema 校验，超时为 600 秒。

Linux 额外实测 4 项：上传文件落盘及摘要检查、动态 IP 替换、脚本自定义端口保留、带空格路径的映射查询。仅在唯一临时目录运行无业务语义的小脚本，退出时删除目录；没有创建业务容器或运行 NPU 工作负载。原始记录为 `data/workflow-launch-acceptance.json`。

Archify 工作流设计参考按 workflow v2 生成，showcase 9/9、零错误与警告；四种桌面尺寸与浅深主题通过浏览器检查并视检。参考保存在 `data/workflow-editor-design.html`，规范 SHA-256 为 `6d980c8b2cccd139a60f2646e3a49725245d49874fc9c88290b140e89e9cb925`，HTML SHA-256 为 `5c7a7710809b882d7300928665e0731e5fe688c737bcf14cbe60ef2b32f7fe22`。实际页面使用可编辑 React/SVG，不把静态设计参考当作编辑器。

增量全量回归：249 项，242 项通过、7 项平台能力限制跳过，耗时 276.325 秒。最终审阅补充 `${input}` 绑定及带 `+` 镜像别名的一致性校验，并通过 23 项专项回归。

新版已迁移 007 映射表并部署 18000。上线核验健康、5 台真实节点、5 个映射查询和静态资源正常；现有映射均为版本 0 的空列表，未创建测试任务或预置。

## 2026-09-13 多节点、草稿与可派生预置

申请页改为机器资源 → 任务环境 → Jobs；环境与 job 各自使用 Tab。任务名带默认时间，生命周期使用明确说明。一个环境可选择多个节点，产物选择环境/容器和多个节点。浏览器按用户名用 IndexedDB 保存输入及上传文本，切页和刷新可恢复；提交成功后清除。没有新增运行依赖。

后端将逻辑配置展开为持久化环境/job 实例；验证了两台模拟 SSH 主机的独立执行、全部上游实例屏障、服务消费者收尾、空间复用、未知关闭时保留资源以及跨节点同名产物下载。`${task_id}`/`${job_id}` 产物路径在实例提交时解析，避免同一空间内不同任务覆盖结果。逻辑模板继续用于另存和复用。

新增真实 MySQL + SSH 边界模拟的完整验证：任务成功 → 管理员发布并启用 → 固定 merged 来源派生个人用例 → 本人再次申请两节点并成功 → 四份独立产物下载；其他用户提交该个人变体得到 403，未访问 GitHub 或创建申请。业务判定仍以外部原始记录为准，发布的“执行成功”不替代性能判定。

本轮全量 277 项测试，270 项通过、7 项平台条件跳过（526.424 秒）；其中包含当时被 unittest 发现的重复导入基类用例，随后已消除该重复发现。后续修改单跑来源 12 项、预置 13 项，以及产物隔离/完整发布闭环 2 项，均通过。前端统一浏览器 27 项通过，补充动态参数说明后生产构建通过；浏览器产生的单节点/多节点真实提交 payload 均通过后端 schema。

已迁移 008 个人变体表，重启 API/worker，部署在 18000。上线检查健康、最新静态资源、5 台节点和预置表正常。指定的 Qwen3 nightly 正按固定上游提交准备实机验收；实际结果另行记录，以上平台测试不作为 NPU 业务通过证据。

预置草案增量：14 项预置回归、排队草案到实际成功后同条启用的完整 SSH 边界闭环以及来源失效授权验证通过；新增待验收页面测试先失败后通过，相关 4 项浏览器回归及生产构建通过。上线以现有普通成员账户核验真实预置可见、13 项标签、merged 来源、清除指定节点约束、禁用状态和最新静态文件。Qwen3 的首次网络失败、源码缓存诊断与重试排队证据见[实机样例记录](nightly-qwen3.md)。
