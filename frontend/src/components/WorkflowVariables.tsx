import type { WorkflowEnvironment } from '../workflowTypes';
export function WorkflowVariables({environments,nodeCount}:{environments:WorkflowEnvironment[];nodeCount:number}) {
  let index=0;
  const rows=environments.flatMap(env=>[...(env.node_aliases||[env.node_alias])].filter(node=>/^node\d+$/.test(node)&&Number(node.slice(4))<nodeCount).sort((a,b)=>Number(a.slice(4))-Number(b.slice(4))).map(node=>({environment:env.alias,node,container:index++})));
  return <section className="workflow-variable-guide" aria-label="变量对应关系"><h4>环境、节点与容器变量</h4><p className="muted small">同一环境在每个选定节点创建一个容器。容器从 0 编号，按环境顺序、再按节点编号排列；表中的变量在执行时替换为实际 IP 和容器名。</p><div className="table-scroll"><table><thead><tr><th>环境</th><th>服务器</th><th>容器编号</th><th>服务器 IP 变量</th><th>容器名变量</th></tr></thead><tbody>{rows.map(row=><tr key={row.container}><td>{row.environment}</td><td>{row.node}</td><td>container{row.container}</td><td><code>HIVE_NODE{Number(row.node.slice(4))}_IP</code></td><td><code>HIVE_CONTAINER{row.container}_NAME</code></td></tr>)}</tbody></table></div><dl className="detail-list"><dt>源码目录</dt><dd><code>HIVE_SOURCE_DIR</code>：当前步骤的源码目录。</dd><dt>任务标识</dt><dd><code>HIVE_TASK_ID</code>：本次任务 ID。</dd><dt>作业标识</dt><dd><code>HIVE_JOB_ID</code>：当前作业实例 ID。</dd></dl><details><summary>查看变量使用示例</summary><p className="muted small">Shell 使用 $变量名，Python 从 os.environ 读取。服务端地址使用对应节点的 IP 变量。</p><pre className="code-editor" style={{whiteSpace:'pre-wrap'}}>{'cd "$HIVE_SOURCE_DIR"\necho "$HIVE_NODE0_IP" "$HIVE_CONTAINER0_NAME"\nmkdir -p "/home/results/$HIVE_TASK_ID/$HIVE_JOB_ID"'}</pre><pre className="code-editor" style={{whiteSpace:'pre-wrap'}}>{'import os\nprint(os.environ["HIVE_SOURCE_DIR"])\nprint(os.environ["HIVE_NODE0_IP"], os.environ["HIVE_CONTAINER0_NAME"])'}</pre></details></section>;
}
