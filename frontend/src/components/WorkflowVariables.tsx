import type { WorkflowJob } from '../workflowTypes';
export function WorkflowVariables({nodeCount,jobs}:{nodeCount:number;jobs:WorkflowJob[]}) {
  const endpoints=jobs.filter(job=>job.kind==='service').map(job=>`HIVE_JOB_${job.id.toUpperCase()}_NODE0_ENDPOINT`);
  return <><p className="muted small">脚本统一使用 <code>HIVE_</code> 环境变量获取当前节点、容器、代码目录和服务地址。同一环境选择多个节点时，会在每个节点独立执行作业。</p><details className="workflow-env-details"><summary>查看 HIVE_ 变量与脚本示例</summary><dl className="detail-list source-snapshot"><dt>节点 IP</dt><dd>{Array.from({length:nodeCount},(_,index)=><code key={index} style={{display:'inline-block',marginRight:12}}>HIVE_NODE{index}_IP</code>)}</dd><dt>当前执行位置</dt><dd><code>HIVE_HOST_IP</code> 当前节点 IP；<code>HIVE_NODEID</code> 当前节点编号（从 0 开始）；<code>HIVE_CONTAINER_NAME</code> 当前真实容器。</dd><dt>全部节点</dt><dd><code>HIVE_JOB_NODELIST</code> 逗号分隔的 IP；<code>HIVE_JOB_NUM_NODES</code> 节点数；<code>HIVE_NODES_JSON</code> 节点结构化信息。</dd><dt>代码与镜像</dt><dd><code>HIVE_SOURCE_DIR</code> 源码目录；<code>HIVE_IMAGE</code> 当前环境镜像。</dd><dt>任务与端口</dt><dd><code>HIVE_TASK_ID</code> 任务 ID；<code>HIVE_JOB_ID</code> 当前作业实例 ID；<code>HIVE_PORT</code> 当前服务端口。</dd><dt>依赖服务地址</dt><dd>{(endpoints.length?endpoints:['HIVE_JOB_SERVE_NODE0_ENDPOINT']).map(value=><code key={value} style={{display:'block'}}>{value}</code>)}在依赖服务就绪后的作业中使用；变量由服务作业 ID 和节点编号组成。</dd><dt>当前节点映射</dt><dd><code>hive_resource model 模型名称</code> 返回当前节点的模型路径；也支持 <code>dataset</code>、<code>image</code>、<code>package</code>。</dd></dl><p className="muted small">Bash</p><pre className="code-editor" style={{whiteSpace:'pre-wrap'}}>{'cd "$HIVE_SOURCE_DIR"\necho "$HIVE_NODE0_IP $HIVE_HOST_IP $HIVE_CONTAINER_NAME"\necho "$HIVE_JOB_NODELIST ($HIVE_JOB_NUM_NODES nodes)"\nmodel_path=$(hive_resource model 模型名称)\nmkdir -p "/home/results/$HIVE_TASK_ID/$HIVE_JOB_ID"'}</pre><p className="muted small">Python</p><pre className="code-editor" style={{whiteSpace:'pre-wrap'}}>{'import json, os\nfor node in json.loads(os.environ["HIVE_NODES_JSON"]):\n    print(node["node_alias"], node["ip"])\nprint(os.environ["HIVE_SOURCE_DIR"], os.environ["HIVE_CONTAINER_NAME"])'}</pre></details></>;
}
